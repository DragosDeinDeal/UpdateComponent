import XCTest
@testable import UpdatePopupComponent

final class UpdatePopupComponentTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(UpdatePopupComponent().text, "Hello, World!")
    }
}
